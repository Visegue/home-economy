allowBuilds:
  # Required by Vite and Drizzle Kit. Keep the allowlist explicit.
  esbuild: true
  sharp: false
  unrs-resolver: false

overrides:
  # Update with better-auth: the adapter's core peer must match the runtime.
  "@better-auth/core": 1.7.4
  esbuild: 0.28.2
  qs: 6.16.0
